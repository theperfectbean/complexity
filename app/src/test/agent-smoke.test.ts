import { describe, it } from "vitest";

describe.skip("Perplexity Agent SMOKE", () => {
  it("verifies basic response from each active model", async () => {
    // Obsolete: test was tightly coupled to Perplexity SDK
  });
});
